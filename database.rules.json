{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "u": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        ".validate": "newData.hasChildren(['v','a','t'])",
        "v": { ".validate": "newData.val() !== ''" },
        "a": { },
        "t": { }
      }
    }
  }
}
